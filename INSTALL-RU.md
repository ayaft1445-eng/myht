# HubMenu — тёмное меню сервера

Отдельный мод. Ставится рядом с SimpleMenu и не мешает ему:
у SimpleMenu команда `/menu`, у этого мода — `/hub`.

---

## Что делает

Команда **`/hub`** открывает окно **HUB** — чёрный фон, белая обводка,
четыре кнопки-карточки:

- **МИНИ-ИГРЫ** — открывает окно со списком режимов;
- **НОВОСТИ** — открывает окно с новостями сервера;
- **ПРАВИЛА** — открывает окно с правилами сервера;
- **ДС СЕРВЕР** — открывает окно со ссылкой на Discord.

В каждом окне раздела внизу кнопки **НАЗАД** и **ЗАКРЫТЬ**, ESC тоже закрывает.
При наведении на карточку она становится белой, а текст — чёрным.

---

## Установка

1. Скачай `HubMenu-1.2.0.jar`: <https://github.com/ayaft1445-eng/myht/releases/latest>
   (файл внизу страницы, в разделе **Assets**).
2. Останови сервер.
3. Положи файл в папку **`plugins`** сервера.
4. Запусти сервер. В консоли должна появиться строка:
   `[HubMenu] Loaded. Type /hub in chat to open the menu.`
5. В игре напиши `/hub`.

Если раньше стоял `ServerHub-1.0.0.jar` — удали его, этот мод пришёл ему на замену.

---

## Что поменять под свой сервер

Папка с разметкой: `src/main/resources/Common/UI/Custom/HubMenu/`

| Что | Файл | Что искать |
|---|---|---|
| **Ссылка на Discord** | `Section_Discord.ui` | `discord.gg/ЗАМЕНИ-НА-СВОЮ-ССЫЛКУ` |
| Подписи на кнопках | `Main.ui` | `Text: "МИНИ-ИГРЫ";`, `Text: "НОВОСТИ";`, `Text: "ДС СЕРВЕР";` (в блоках `Label`) |
| Ширина кнопок | `Main.ui` | `Anchor: (Width: 340, ...)` — у каждой карточки своя, сумма с отступами должна влезать в ширину окна |
| Заголовок HUB | `Main.ui` | `Text: "HUB";` |
| Размер окна | любой файл | `Anchor: (Width: 1280, Height: 620);` в начале |
| Прозрачность фона | любой файл | число в скобках: `#000000(0.60)` — меньше значит прозрачнее |
| Подпись на карточке | `Main.ui` | `Label` с `Bottom: 20` — отступ подписи от низа |
| Толщина белой обводки | любой файл | полоски `Group { Anchor: (Height: 2); Background: #ffffff; }` и `(Width: 2)` |
| Текст новостей | `Section_News.ui` | строки `Text: "...";` |
| Режимы мини-игр | `Section_Minigames.ui` | строки `Text: "...";` |
| Название команды | `src/main/java/dev/hytalemodding/hubmenu/commands/HubCommand.java` | `super("hub", ...)` |

Рамка собрана из четырёх тонких полосок по краям. Так сделано специально: если
залить цветом всю подложку, полупрозрачная заливка смешается с ней и станет светлее.
Хочешь рамку толще — поменяй `2` на `3` в полосках.

После правок: залей файлы в репозиторий → вкладка **Actions** → дождись зелёной
галочки → свежий jar появится в Releases.

---

## Палитра

Тёмно-фиолетовый стиль: почти чёрный фон с фиолетовым отливом, тонкие рамки,
белый капс, яркий фиолетовый акцент.

| Что | Код |
|---|---|
| Фон окна | подложка `#130c22` (0.90) + переход `#010002` → `#251640` (0.5) |
| Фон карточек | подложка `#1e1230` (0.78) + переход `#0a0614` → `#321e4e` (0.5) |
| Рамка выделенной карточки | `#4a3768` |
| Рамка остальных | `#3a2a55` |
| Акцент (черта под заголовком, ESC, метки) | `#a35dff` |
| Наведение на карточку | `#a35dff` с прозрачностью 0.16 |
| Текст | `#ffffff`, описания `#bfb2d9`, приглушённое `#6f6189` |

Фон собран в два слоя: снизу сплошная заливка, поверх неё — десять
полупрозрачных полос с промежуточными оттенками, они и дают переход цвета
сверху вниз. Одними полосами фон не собрать: на масштабе клиента между ними
остаются щели в пиксель и через них просвечивает мир — получаются зелёные
линии поперёк меню. Со сплошной подложкой в такой щели видно её же.

Прежняя тёмно-красная версия с такой же раскладкой лежит в папке
`palettes/dark-red` — если захочешь её, скопируй файлы оттуда в
`src/main/resources/Common/UI/Custom/HubMenu` и пересобери.

---

## Из чего состоит

```
src/main/java/dev/hytalemodding/hubmenu/
├── HubMenuPlugin.java         команда /hub и показ выбора языка при входе
├── commands/HubCommand.java   сама команда
├── lang/MenuLanguage.java     языки меню и пути к разметке
├── lang/LanguageStore.java    выбор языка каждого игрока, файл languages.properties
└── ui/HubMenuPage.java        какое окно показывать

src/main/resources/
├── manifest.json              описание плагина для сервера
└── Common/UI/Custom/HubMenu/
    ├── Main_ru.ui             главное окно, русский
    ├── Main_en.ui             главное окно, английский
    ├── Section_Minigames_ru.ui / _en.ui   окно «Мини-игры»
    ├── Section_News_ru.ui / _en.ui        окно «Новости»
    ├── Section_Rules_ru.ui / _en.ui       окно «Правила»
    ├── Section_Language.ui    окно выбора языка, подписи на двух языках
    └── Section_Discord.ui     окно «ДС сервер», сейчас в меню не показывается
```

## Языки

Игрок, зашедший на сервер впервые, сразу видит окно выбора языка: русский или
английский. Выбор запоминается в `languages.properties` в папке данных плагина
(строка вида `uuid=ru`), поэтому переживает перезапуск сервера. Поменять язык
можно в любой момент — четвёртая карточка главного меню «Язык».

Текст меню лежит прямо в разметке: русские строки в файлах `*_ru.ui`, английские
в `*_en.ui`. Меняешь текст — меняй в обоих.
